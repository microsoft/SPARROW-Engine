# pw-tools: Build-time manifest generation toolkit for PytorchWildlife.
#
# NOT part of the pytorchwildlife runtime package. Lives in tools/ to enforce
# the AGPL boundary: torch, onnx, huggingface_hub are build-time-only deps.
#
# Install with the [tools] extra defined in pyproject.toml:
#   pip install pytorchwildlife[tools]
#
# Entry point: pw-tools (see pyproject.toml [project.scripts])
